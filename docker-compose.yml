services:
  home-ops:
    build: .
    env_file: .env
    volumes:
      - duckdb_data:/data/db
      - snapshots_data:/data/snapshots
      - ./user_profile.yml:/app/user_profile.yml:ro
    command: ["scan"]

volumes:
  duckdb_data:
  snapshots_data:
